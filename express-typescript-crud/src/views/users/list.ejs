<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>User List</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-light">
    <div class="container py-5">
      <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="fw-bold text-primary">Quản lý người dùng</h2>
        <a href="/users/create" class="btn btn-success">➕ Thêm người dùng mới</a>
      </div>

      <div class="card shadow-lg rounded-4">
        <div class="card-body">
          <table class="table table-hover align-middle">
            <thead class="table-primary">
              <tr>
                <th>#</th>
                <th>Tên</th>
                <th>Email</th>
                <th>Tuổi</th>
                <th>Hành động</th>
              </tr>
            </thead>
            <tbody>
              <% if (users.length === 0) { %>
                <tr>
                  <td colspan="5" class="text-center text-muted py-4">
                    No users found.
                  </td>
                </tr>
              <% } else { %>
                <% users.forEach((u, i) => { %>
                  <tr>
                    <td><%= i + 1 %></td>
                    <td><%= u.name %></td>
                    <td><%= u.email %></td>
                    <td><%= u.age || '-' %></td>
                    <td>
                      <a href="/users/edit/<%= u._id %>" class="btn btn-sm btn-warning me-2">✏️ Chỉnh sửa</a>
                      <a href="/users/delete/<%= u._id %>"
                         onclick="return confirm('Are you sure?')"
                         class="btn btn-sm btn-danger">🗑️ Xóa người dùng</a>
                    </td>
                  </tr>
                <% }) %>
              <% } %>
            </tbody>
          </table>
        </div>
      </div>

      <div class="text-center mt-4">
        <a href="/" class="btn btn-secondary">⬅️ Quay lại trang chủ</a>
      </div>
    </div>
  </body>
</html>
